import React, { render } from 'react';
let Root = React.createClass({	
    render() {
        return (
            <div>
            	hello, react! hhaha
            </div>
        );
    }
});

export default Root;
